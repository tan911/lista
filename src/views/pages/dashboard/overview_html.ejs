<%- include(template.render_layouts+'/dashboard_html'); -%>

<main class="w-full px-5 max-w-[1440px] md:mx-auto md:px-10">
    <%- include(template.render_overview+'segmented_control_html') -%>
    <!-- metrics -->
    <%- include(template.render_overview+'card_metrics_html') -%>
    <div class="flex flex-col gap-5 mt-5 md:flex-row md:justify-between md:gap-10">
        <div
            id="container"
            class="bg-white w-full h-[448px] max-w-[790px] rounded-xl border border-b-['#E0E0E0'] shadow-sm"
        ></div>
        <%- include(template.render_overview+'top_customer_html') -%>
    </div>
</main>

<%- include(template.render_dialogs+'add_credit_html'); -%>
